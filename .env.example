ELEVENLABS_API_KEY=
ANTHROPIC_API_KEY=
HOST_VOICE_ID=onwK4e9ZLuTAKqWW03F9
PARTICIPANT_VOICE_ID=21m00Tcm4TlvDq8ikWAM
PORT=3001
# Comma-separated list of allowed origins. In production, set to your Railway URL.
CORS_ORIGINS=http://localhost:5173
# Optional: override the Claude model. Defaults to current Sonnet.
ANTHROPIC_MODEL=claude-sonnet-4-6
# Shared secret gating the paid routes (/api/turn, /api/transcribe, /api/tts).
# Leave unset for frictionless local dev. SET THIS in production so a public
# deployment can't be spammed into running up the ElevenLabs/Anthropic bill.
# Players enter it on the landing screen.
APP_PASSWORD=
